import { Config } from "./model/config";
export declare class CucumberConverter {
    private config;
    constructor(config: Config);
    convertToJunit(inputFilePath: string, outputFilePath: string): void;
    private addTestSuite;
    private addTestCases;
    private getScenarioResult;
}
